@model SalesWebMvc.Models.Seller

@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Are your sure you want to delete this?</h3>


<div>
    <h4>Seller</h4>
    <hr />
    <dl>
        <dt>@Html.DisplayNameFor(modelItem => modelItem.Name)</dt>
        <dd>@Html.DisplayFor(modelItem => modelItem.Name)</dd>

        <dt>@Html.DisplayNameFor(modelItem => modelItem.Email)</dt>
        <dd>@Html.DisplayFor(modelItem => modelItem.Email)</dd>

        <dt>@Html.DisplayNameFor(modelItem => modelItem.BirthDate)</dt>
        <dd>@Html.DisplayFor(modelItem => modelItem.BirthDate)</dd>

        <dt>@Html.DisplayNameFor(modelItem => modelItem.BaseSalary)</dt>
        <dd>@Html.DisplayFor(modelItem => modelItem.BaseSalary)</dd>
    </dl>
</div>
<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <input type="submit" value="Delete" class="btn btn-danger" />
    <a asp-action="Index">Back to List</a>
</form>
</div>